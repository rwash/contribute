<h3><%= list.title %></h3>
<div class="row group-tiles">
  <ul class="thumbnails-small">
    <% for project in list.get_projects_in_order(5) %>
      <li class="project-span">
      	<%= render :partial => partial, :locals => { :project => project } %><br>
      </li>  
    <% end %>
  </ul>
</div>
<%= link_to "More", list %>