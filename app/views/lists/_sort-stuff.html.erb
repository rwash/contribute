<style >
  .handle:hover{cursor: move;}
</style>
<%= javascript_tag do %>
  // Sorting the list
  $(document).ready(function()
  {
    $('#listings').sortable(
    {
      axis: 'x',
      dropOnEmpty: false,
      handle: '.handle',
      cursor: 'crosshair',
      items: 'li',
      opacity: 0.4,
      scroll: true,
      update: function()
      {
        $.ajax(
        {
          type: 'post',
          data: $('#listings').sortable('serialize') + "&title=" + $('#title').val() + "&authenticity_token=" + "<%= form_authenticity_token %>",
          dataType: 'script',
          complete: function(request)
          {
            $('#listings').effect('highlight');
          },
          url: '<%= "/lists/#{id}/listings/sort" %>'
        })
      }
    });
  });
<% end %>
