<div id="projectPreview">

	<div class="floatRight">
		<div class="progressBar"><%= project.contributions_percentage %></div>
	</div>

	<%= link_to project.name, project %><br>

	<div align="center">
		<% if project.picture? %>
			<%= image_tag project.picture.url(:thumb), :class => "projectPreviewImage" %>
		<% else %>
			<%= image_tag "BlockSThumb.png", :class => "projectPreviewImage" %>
		<% end %>
	</div>

	<p>By: <%= project.user.name %></p>
	<%= project.short_description %><br>

	<div class="project_footer">
		<ul>
			<li><%= project.end_date %></li>
			<li><%= number_to_currency(project.contributions_total, :precision => 0) %> / <%= number_to_currency(project.funding_goal, :precision => 0) %></li>
		</ul>

		<ul>
			<li>End Date</li>
			<li>Pledged</li>
		</ul>
  </div>

</div>
