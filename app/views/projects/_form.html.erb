<%= form_for(@project) do |f| %>
	<% if @project.errors.any? %>
		<div id="error_explanation">
		<h2><%= pluralize(@project.errors.count, "error") %> prevented this project from being saved:</h2>
		<ul>
		<% @project.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
		</div>
	<% end %>
Name: <%= f.text_field :name  %><br>
Short Desc: <%= f.text_field :shortDescription %><br>
Long Desc:<br>
<%= f.text_area :longDescription, :size => "40x10" %><br>
End Date: <%= f.text_field :endDate, :size => 10 %> <br>
Category: <%= f.collection_select :categoryId, Category.all, :id, :shortDescription %><br>
Picture: (optional) <%= f.file_field :picture %> <br>
Funding Goal: $<%= f.text_field :fundingGoal, :size => 10 %><br>
<%= f.submit %>
<% end %>
