<%= form_for(@project) do |f| %>
	<% if @project.errors.any? %>
		<div id="error_explanation">
		<h2><%= pluralize(@project.errors.count, "error") %> prohibited this product from being saved:</h2>
		<ul>
		<% @project.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
		</div>
	<% end %>
Name: <%= f.text_field :name  %><br />
Short Desc: <%= f.text_field :shortDescription %><br />
Long Desc: <%= f.text_field :longDescription %><br />
End Date: <%= f.text_field :endDate %> TODO: GET THIS TO WORK!<br />
Category: <%= f.collection_select :categoryId, Category.all, :id, :shortDescription %><br />
Funding Goal: <%= f.text_field :fundingGoal %><br />
<%= f.submit %>
<% end %>
