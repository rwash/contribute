<%= form_for(@amazon_payment_account) do |f| %>
  <% if @amazon_payment_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amazon_payment_account.errors.count, "error") %> prohibited this amazon_payment_account from being saved:</h2>

      <ul>
      <% @amazon_payment_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
