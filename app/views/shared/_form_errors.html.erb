<% if subject.errors.any? %>
  <div id="error_explanation">
  <h2>The form had <%= pluralize(subject.errors.count, "error") %>:</h2>
  <ul>
  <% subject.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
  </div>
<% end %>
